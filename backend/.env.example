# Base de datos
DB_URL=jdbc:mysql://localhost:3306/inventario_db?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=root

# JWT Configuration
JWT_SECRET=CAMBIAR_ESTE_SECRET_POR_UNO_SEGURO_DE_AL_MENOS_32_CARACTERES
JWT_EXPIRATION=86400000

# CORS Configuration
# Orígenes permitidos separados por comas (sin espacios)
# Para desarrollo local: http://localhost:4200
# Para múltiples orígenes: http://localhost:4200,https://app.example.com
# IMPORTANTE: NO usar "*" cuando allowCredentials=true
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Server
SERVER_PORT=8080
